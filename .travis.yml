sudo: false # use container-based build
language: fortran
notifications:
    email: false

compiler: gfortran
os: linux
env: TESTID='rbm_linux'

before_install:
  - source ci/rbm_install_util
  - rbm_before_install
install:
   - rbm_install
script:
  - rbm_script
after_success:
  - rbm_after_success
after_failure:
  - rbm_after_failure
